<%= render partial: 'posts/posts_navbar' %>

<% @posts.each do |post| %>

   (<%= post.id %>) 
   <%= post.date %> : <%= post.picture_short_name %> 
   <%= link_to post.title, post_path(post) %><br>
<% end %>